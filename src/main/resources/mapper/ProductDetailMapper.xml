<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.ProductDetailMapper">

    <!-- ==================================================
         DETAIL01_VIEWCOUNT
         조회수 증가
    ================================================== -->
    <update id="increaseViewCount" parameterType="long">
        UPDATE products
        SET view_count = IFNULL(view_count, 0) + 1
        WHERE product_id = #{productId}
    </update>


    <!-- ==================================================
         DETAIL01_INFO
         상품 상세 기본 정보
         (이미지는 여기서 조회 ❌)
    ================================================== -->
    <select id="selectProductDetail"
        parameterType="long"
        resultType="com.itwillbs.dto.ProductDetailDTO">

    SELECT
        /* ===== 상품 기본 ===== */
        p.product_id           AS productId,
        p.product_name         AS productName,
        p.price                AS price,
        p.description          AS description,

        /* 대표 이미지 */
        p.main_image_url       AS mainImageUrl,

        /* 통계 */
        p.view_count           AS viewCount,
        p.like_count           AS likeCount,
        p.created_at           AS createdAt,
        p.condition_status     AS conditionStatus,
        p.sales_status         AS salesStatus,
        p.region_display_name  AS sellerRegion,

        /* ===== 카테고리 (추가) ===== */
        c.category_id          AS categoryId,
        c.name                 AS categoryName,
        pc.category_id         AS parentCategoryId,
        pc.name                AS parentCategoryName

    FROM products p
    JOIN categories c
      ON p.category_id = c.category_id
    LEFT JOIN categories pc
      ON c.parent_id = pc.category_id

    WHERE p.product_id = #{productId}

</select>


<!-- ==================================================
     DETAIL01_SELLER_INFO
     판매자 정보 (상품 상세)
================================================== -->
<select id="selectSellerInfo"
        parameterType="long"
        resultType="com.itwillbs.dto.ProductSellerInfoDTO">

    SELECT
    u.user_id              AS sellerId,
    u.nickname             AS nickname,
    COUNT(p.product_id)    AS productCount,
    AVG(r.rating)          AS rating
FROM users u
LEFT JOIN products p
       ON p.seller_id = u.user_id
LEFT JOIN reviews r
       ON r.seller_id = u.user_id
WHERE u.user_id = (
    SELECT seller_id
    FROM products
    WHERE product_id = #{productId}
)
GROUP BY u.user_id, u.nickname

</select>


    <!-- ==================================================
         DETAIL01_IMAGES
         상품 상세 이미지 목록 ⭐⭐⭐
    ================================================== -->
    <select id="selectProductImages"
            parameterType="long"
            resultType="string">

        SELECT
            image_url
        FROM product_images
        WHERE product_id = #{productId}
        ORDER BY sort_order ASC

    </select>


<!-- DETAIL01_SIMILAR_1
     같은 소분류 기준 -->
<select id="selectSimilarBySubCategory"
        parameterType="map"
        resultType="com.itwillbs.dto.ProductSimilarDTO">

  SELECT
      p.product_id          AS productId,
      p.product_name        AS productName,
      p.price               AS price,
      p.main_image_url      AS thumbnailUrl,
      p.region_display_name AS regionName
  FROM products p
  WHERE p.category_id = (
      SELECT category_id
      FROM products
      WHERE product_id = #{productId}
  )
  AND p.product_id != #{productId}
  ORDER BY p.created_at DESC
  LIMIT #{limit}

</select>

    <!-- ==================================================
         DETAIL01_SIMILAR (fallback)
         인기 상품 대체
    ================================================== -->
    <select id="selectPopularProductsForSimilar"
            parameterType="int"
            resultType="com.itwillbs.dto.ProductSimilarDTO">

        SELECT
            p.product_id           AS productId,
            p.product_name         AS title,
            p.price                AS price,
            p.main_image_url       AS thumbnailUrl,
            p.region_display_name  AS regionName,
            (
                SELECT COUNT(*)
                FROM user_favorites uf
                WHERE uf.product_id = p.product_id
            ) AS likeCount
        FROM products p
        ORDER BY likeCount DESC
        LIMIT #{limit}

    </select>
    
    <!-- DETAIL01_SIMILAR_2
     같은 대분류 기준 (fallback) -->
<select id="selectSimilarByParentCategory"
        parameterType="map"
        resultType="com.itwillbs.dto.ProductSimilarDTO">

  SELECT
      p.product_id          AS productId,
      p.product_name        AS productName,
      p.price               AS price,
      p.main_image_url      AS thumbnailUrl,
      p.region_display_name AS regionName
  FROM products p
  JOIN categories c
    ON p.category_id = c.category_id

  WHERE c.parent_id = (
      SELECT c2.parent_id
      FROM products p2
      JOIN categories c2
        ON p2.category_id = c2.category_id
      WHERE p2.product_id = #{productId}
  )

  AND p.product_id != #{productId}

  <if test="excludeIds != null and excludeIds.size() > 0">
    AND p.product_id NOT IN
    <foreach item="id"
             collection="excludeIds"
             open="(" separator="," close=")">
        #{id}
    </foreach>
  </if>

  ORDER BY p.created_at DESC
  LIMIT #{limit}

</select>

</mapper>
