<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.UserMapper">

    <select id="findSocialAccount" resultType="com.itwillbs.dto.SocialAccountDTO">
        SELECT 
            social_id AS socialId, 
            connected_at AS connectedAt, 
            provider, 
            provider_id AS providerId, 
            user_id AS userId
        FROM social_accounts 
        WHERE provider = #{provider} AND provider_id = #{providerId}
    </select>

<insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
    INSERT INTO users (
        username, 
        password,   nickname, 
        role, 
        email, 
        user_status, 
        phone_number, 
        profile_img, 
        created_at
    ) VALUES (
        #{username}, 
        #{password},   #{nickname}, 
        'ROLE_USER', 
        #{email}, 
        'ACTIVE', 
        #{phoneNumber}, 
        'default.png', 
        NOW()
    )
</insert>

<select id="findUserByEmail" resultType="map">
    SELECT user_id AS userId FROM users WHERE email = #{email}
</select>


<insert id="insertSocialAccount">
    INSERT INTO social_accounts (
        social_id, connected_at, provider, provider_id, user_id
    )
    VALUES (
        #{socialId}, NOW(), #{provider}, #{providerId}, #{userId}
    )
</insert>

</mapper>