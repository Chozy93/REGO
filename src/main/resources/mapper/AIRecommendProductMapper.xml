<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.ai.AIRecommendProductMapper">

    <select id="selectByIds"
        parameterType="java.util.List"
        resultType="com.itwillbs.ai.AIRecommendProductDTO">

    SELECT
        p.product_id     AS productId,
        p.product_name   AS title,
        p.price          AS price,
        p.main_image_url AS thumbnail
    FROM products p
    WHERE p.product_id IN
    <foreach collection="ids"
             item="id"
             open="("
             separator=","
             close=")">
        #{id}
    </foreach>

</select>

<select id="selectAIProducts"
        resultType="com.itwillbs.ai.AIRecommendProductDTO">
    SELECT
        product_id     AS productId,
        product_name   AS title,
        price,
        main_image_url AS thumbnail
    FROM products
    WHERE sales_status = 'ON_SALE'
    ORDER BY created_at DESC   <!-- ⭕ 최근성 기준 -->
    LIMIT 10
</select>



</mapper>
